{
  "name": "data-structures",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:coverage": "npm test -- --coverage",
    "test:watch": "jest --watch --coverage",
    "test:badges": "npm run test:coverage && jest-coverage-badges && mv coverage/**.svg ./"
  },
  "jest": {
    "coverageReporters": [
      "json-summary",
      "text",
      "lcov"
    ]
  },
  "devDependencies": {
    "jest": "^26.5.3"
  },
  "dependencies": {
    "jest-coverage-badges": "^1.1.2"
  }
}
